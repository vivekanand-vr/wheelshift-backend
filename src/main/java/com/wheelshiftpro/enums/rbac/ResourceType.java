package com.wheelshiftpro.enums.rbac;

/**
 * Enum representing resource types for ACL (Access Control List).
 */
public enum ResourceType {
    CAR,
    CLIENT,
    INQUIRY,
    RESERVATION,
    SALE,
    TRANSACTION
}
