package com.wheelshiftpro.enums;

/**
 * Enum representing whether a scope should include or exclude data.
 */
public enum ScopeEffect {
    /**
     * Include data matching the scope
     */
    INCLUDE,
    
    /**
     * Exclude data matching the scope
     */
    EXCLUDE
}
