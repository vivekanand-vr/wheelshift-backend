package com.wheelshiftpro.enums;

/**
 * Enum representing the type of subject in ACL entries.
 */
public enum SubjectType {
    /**
     * Subject is a role
     */
    ROLE,
    
    /**
     * Subject is an employee
     */
    EMPLOYEE
}
